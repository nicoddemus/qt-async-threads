[build-system]
requires = [
  "setuptools",
  "setuptools-scm[toml]",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100

[tool.pytest.ini_options]
addopts = "-ra"
pythonpath = "docs"
